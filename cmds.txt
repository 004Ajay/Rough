✅ One-time setup: Add this to config.fish

function abbrp
    set name $argv[1]
    set value $argv[2..-1]
    set line "abbr --add $name '$value'"
    if not grep -q "$line" ~/.config/fish/abbrs.fish
        echo $line >> ~/.config/fish/abbrs.fish
        source ~/.config/fish/abbrs.fish
        echo "Saved: $line"
    else
        echo "Already exists: $line"
    end
end

This defines a new command called abbrp (short for “abbreviation persistent”).
🔧 How to use it from now on:

Instead of:

abbr --add ca 'conda activate'

You’ll run:

abbrp ca 'conda activate'

This will:

    Add it to ~/.config/fish/abbrs.fish

    Avoid duplicates

    Source it immediately
    
    
    
# General Commands
abbr --add s 'sudo'
abbr --add upd 'sudo apt-get update'
abbr --add upg 'sudo apt-get upgrade'

# Conda Commands
abbr --add ca 'conda activate'
abbr --add cde 'conda deactivate'
abbr --add cenv 'conda env list'
abbr --add ccr 'conda create -n <env_name> python=3.11 -y'
abbr --add cexp 'conda env export --name <env_name> > <filename>.yml'
abbr --add crm 'conda remove -n <env_name> --all -y'

# SSH Commands
abbr --add sr 'ssh appuser@192.168.1.202'
abbr --add sl 'ssh appuser@192.168.1.200'

# Docker Commands
abbr --add dps 'sudo docker ps'
abbr --add di 'sudo docker images'
abbr --add dex 'sudo docker exec -it'
abbr --add ds 'sudo docker stop'
abbr --add dr 'sudo docker rm'
abbr --add dri 'sudo docker rmi'

# Python Commands
abbr --add pyt 'python'
abbr --add p3 'python3'
abbr --add pi 'pip install'
abbr --add pu 'pip uninstall'

# NVIDIA Commands
abbr --add ns 'nvidia-smi'
abbr --add wns 'watch nvidia-smi'
abbr --add nvc 'nvcc --version'

# Other Commands
abbr --add c. 'code .'
abbr --add fi 'find ./<filename> -type f > <filename>.txt'
abbr --add pcopy 'time cat <filename>.txt | parallel -j 10 -X -n 1 rsync -av --inplace {} ./<dest_dir_name>'
    
    
    
    
    
    
